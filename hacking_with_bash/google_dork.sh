#!/bin/bash

# Function to perform Google dorking for the target domain
perform_dorking() {
    # Prompt for target domain
    read -p "Enter your target domain (e.g., example.com): " target_domain

    # Construct dork queries for the target domain
    queries=(
        "site:${target_domain} inurl:login"
        "site:${target_domain} intitle:index.of"
        "site:${target_domain} intext:password"
        "site:${target_domain} filetype:pdf"
        "site:${target_domain} inurl:admin"
        "site:${target_domain} intitle:admin"
        "site:${target_domain} intitle:dashboard"
        "site:${target_domain} intitle:config OR site:${target_domain} intitle:configuration"
        "site:${target_domain} intitle:setup"
        "site:${target_domain} intitle:phpinfo"
        "site:${target_domain} inurl:wp-admin"
        "site:${target_domain} inurl:wp-content"
        "site:${target_domain} inurl:wp-includes"
        "site:${target_domain} inurl:wp-login"
        "site:${target_domain} inurl:wp-config"
        "site:${target_domain} inurl:wp-config.txt"
        "site:${target_domain} inurl:wp-config.php"
        "site:${target_domain} inurl:wp-config.php.bak"
        "site:${target_domain} inurl:wp-config.php.old"
        "site:${target_domain} inurl:wp-config.php.save"
        "site:${target_domain} inurl:wp-config.php.swp"
        "site:${target_domain} inurl:wp-config.php~"
        "site:${target_domain} inurl:wp-config.bak"
        "site:${target_domain} inurl:wp-config.old"
        "site:${target_domain} inurl:wp-config.save"
        "site:${target_domain} inurl:wp-config.swp"
        "site:${target_domain} inurl:wp-config~"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:credentials"
        "site:${target_domain} inurl:connectionstrings"
        "site:${target_domain} inurl:secret_key"
        "site:${target_domain} inurl:api_key"
        "site:${target_domain} inurl:client_secret"
        "site:${target_domain} inurl:auth_key"
        "site:${target_domain} inurl:access_key"
        "site:${target_domain} inurl:backup"
        "site:${target_domain} inurl:dump"
        "site:${target_domain} inurl:logs"
        "site:${target_domain} inurl:conf"
        "site:${target_domain} inurl:db"
        "site:${target_domain} inurl:sql"
        "site:${target_domain} inurl:root"
        "site:${target_domain} inurl:confidential"
        "site:${target_domain} inurl:database"
        "site:${target_domain} inurl:passed"
        "site:${target_domain} inurl:login"
        "site:${target_domain} intitle:index.of"
        "site:${target_domain} intext:password"
        "site:${target_domain} filetype:pdf"
        "site:${target_domain} inurl:admin"
        "site:${target_domain} intitle:admin"
        "site:${target_domain} intitle:dashboard"
        "site:${target_domain} intitle:config OR site:${target_domain} intitle:configuration"
        "site:${target_domain} intitle:setup"
        "site:${target_domain} intitle:phpinfo"
        "site:${target_domain} inurl:wp-admin"
        "site:${target_domain} inurl:wp-content"
        "site:${target_domain} inurl:wp-includes"
        "site:${target_domain} inurl:wp-login"
        "site:${target_domain} inurl:wp-config"
        "site:${target_domain} inurl:wp-config.txt"
        "site:${target_domain} inurl:wp-config.php"
        "site:${target_domain} inurl:wp-config.php.bak"
        "site:${target_domain} inurl:wp-config.php.old"
        "site:${target_domain} inurl:wp-config.php.save"
        "site:${target_domain} inurl:wp-config.php.swp"
        "site:${target_domain} inurl:wp-config.php~"
        "site:${target_domain} inurl:wp-config.bak"
        "site:${target_domain} inurl:wp-config.old"
        "site:${target_domain} inurl:wp-config.save"
        "site:${target_domain} inurl:wp-config.swp"
        "site:${target_domain} inurl:wp-config~"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:credentials"
        "site:${target_domain} inurl:connectionstrings"
        "site:${target_domain} inurl:secret_key"
        "site:${target_domain} inurl:api_key"
        "site:${target_domain} inurl:client_secret"
        "site:${target_domain} inurl:auth_key"
        "site:${target_domain} inurl:access_key"
        "site:${target_domain} inurl:backup"
        "site:${target_domain} inurl:dump"
        "site:${target_domain} inurl:logs"
        "site:${target_domain} inurl:conf"
        "site:${target_domain} inurl:db"
        "site:${target_domain} inurl:sql"
        "site:${target_domain} inurl:root"
        "site:${target_domain} inurl:confidential"
        "site:${target_domain} inurl:database"
        "site:${target_domain} inurl:passed"
        "site:${target_domain} inurl:passwords"
        "site:${target_domain} inurl:passwd"
        "site:${target_domain} inurl:pass"
        "site:${target_domain} inurl:secret"
        "site:${target_domain} inurl:secrets"
        "site:${target_domain} inurl:key"
        "site:${target_domain} inurl:keys"
        "site:${target_domain} inurl:private"
        "site:${target_domain} inurl:priv"
        "site:${target_domain} inurl:ftp"
        "site:${target_domain} inurl:ssh"
        "site:${target_domain} inurl:rdp"
        "site:${target_domain} inurl:sftp"
        "site:${target_domain} inurl:config"
        "site:${target_domain} inurl:cfg"
        "site:${target_domain} inurl:conf"
        "site:${target_domain} inurl:configuration"
        "site:${target_domain} inurl:settings"
        "site:${target_domain} inurl:db"
        "site:${target_domain} inurl:sql"
        "site:${target_domain} inurl:database"
        "site:${target_domain} inurl:data"
        "site:${target_domain} inurl:backups"
        "site:${target_domain} inurl:backup"
        "site:${target_domain} inurl:dump"
        "site:${target_domain} inurl:logs"
        "site:${target_domain} inurl:log"
        "site:${target_domain} inurl:temp"
        "site:${target_domain} inurl:tmp"
        "site:${target_domain} inurl:archive"
        "site:${target_domain} inurl:cached"
        "site:${target_domain} inurl:confidential"
        "site:${target_domain} inurl:admin"
        "site:${target_domain} inurl:administrator"
        "site:${target_domain} inurl:login"
        "site:${target_domain} inurl:signup"
        "site:${target_domain} inurl:register"
        "site:${target_domain} inurl:password"
        "site:${target_domain} inurl:user"
        "site:${target_domain} inurl:account"
        "site:${target_domain} inurl:member"
        "site:${target_domain} inurl:profile"
        "site:${target_domain} inurl:edit"
        "site:${target_domain} inurl:upload"
        "site:${target_domain} inurl:submit"
        "site:${target_domain} inurl:create"
        "site:${target_domain} inurl:delete"
        "site:${target_domain} inurl:remove"
        "site:${target_domain} inurl:adminpanel"
        "site:${target_domain} inurl:panel"
        "site:${target_domain} inurl:webpanel"
        "site:${target_domain} inurl:webadmin"
        "site:${target_domain} inurl:administratorlogin"
        "site:${target_domain} inurl:admin_login"
        "site:${target_domain} inurl:admin-login"
        "site:${target_domain} inurl:admin_login"
        "site:${target_domain} inurl:adminlogin"
        "site:${target_domain} inurl:adminarea"
        "site:${target_domain} inurl:admin_area"
        "site:${target_domain} inurl:adminarea"
        "site:${target_domain} inurl:admin-area"
        "site:${target_domain} inurl:admincp"
        "site:${target_domain} inurl:admincp"
        "site:${target_domain} inurl:admin-cp"
        "site:${target_domain} inurl:admin/cp"
        "site:${target_domain} inurl:admincontrol"
        "site:${target_domain} inurl:admin-control"
        "site:${target_domain} inurl:admincontrol"
        "site:${target_domain} inurl:admin-control"
        "site:${target_domain} inurl:admin-console"
        "site:${target_domain} inurl:admin-console"
        "site:${target_domain} inurl:admin-console"
        "site:${target_domain} inurl:admin-console"
        "site:${target_domain} inurl:adminpage"
        "site:${target_domain} inurl:admin-page"
        "site:${target_domain} inurl:adminpage"
        "site:${target_domain} inurl:admin-page"
        "site:${target_domain} inurl:adminp"
        "site:${target_domain} inurl:admin-p"
        "site:${target_domain} inurl:adminp"
        "site:${target_domain} inurl:admin-p"
        "site:${target_domain} inurl:administrador"
        "site:${target_domain} inurl:administrador"
        "site:${target_domain} inurl:administrator"
        "site:${target_domain} inurl:adm"
        "site:${target_domain} inurl:panel"
        "site:${target_domain} inurl:login"
        "site:${target_domain} inurl:signin"
        "site:${target_domain} inurl:signup"
        "site:${target_domain} inurl:logon"
        "site:${target_domain} inurl:passwd"
        "site:${target_domain} inurl:password"
        "site:${target_domain} inurl:auth"
        "site:${target_domain} inurl:authenticate"
        "site:${target_domain} inurl:authentication"
        "site:${target_domain} inurl:resetpassword"
        "site:${target_domain} inurl:passwordreset"
        "site:${target_domain} inurl:change_password"
        "site:${target_domain} inurl:user"
        "site:${target_domain} inurl:users"
        "site:${target_domain} inurl:member"
        "site:${target_domain} inurl:members"
        "site:${target_domain} inurl:profile"
        "site:${target_domain} inurl:login.php"
        "site:${target_domain} inurl:signin.php"
        "site:${target_domain} inurl:signup.php"
        "site:${target_domain} inurl:logon.php"
        "site:${target_domain} inurl:passwd.php"
        "site:${target_domain} inurl:password.php"
        "site:${target_domain} inurl:auth.php"
        "site:${target_domain} inurl:authenticate.php"
        "site:${target_domain} inurl:authentication.php"
        "site:${target_domain} inurl:resetpassword.php"
        "site:${target_domain} inurl:passwordreset.php"
        "site:${target_domain} inurl:change_password.php"
        "site:${target_domain} inurl:user.php"
        "site:${target_domain} inurl:users.php"
        "site:${target_domain} inurl:member.php"
        "site:${target_domain} inurl:members.php"
        "site:${target_domain} inurl:profile.php"
        "site:${target_domain} inurl:login.aspx"
        "site:${target_domain} inurl:signin.aspx"
        "site:${target_domain} inurl:signup.aspx"
        "site:${target_domain} inurl:logon.aspx"
        "site:${target_domain} inurl:passwd.aspx"
        "site:${target_domain} inurl:password.aspx"
        "site:${target_domain} inurl:auth.aspx"
        "site:${target_domain} inurl:authenticate.aspx"
        "site:${target_domain} inurl:authentication.aspx"
        "site:${target_domain} inurl:resetpassword.aspx"
        "site:${target_domain} inurl:passwordreset.aspx"
        "site:${target_domain} inurl:change_password.aspx"
        "site:${target_domain} inurl:user.aspx"
        "site:${target_domain} inurl:users.aspx"
        "site:${target_domain} inurl:member.aspx"
        "site:${target_domain} inurl:members.aspx"
        "site:${target_domain} inurl:profile.aspx"
        "site:${target_domain} inurl:login.html"
        "site:${target_domain} inurl:signin.html"
        "site:${target_domain} inurl:signup.html"
        "site:${target_domain} inurl:logon.html"
        "site:${target_domain} inurl:passwd.html"
        "site:${target_domain} inurl:password.html"
        "site:${target_domain} inurl:auth.html"
        "site:${target_domain} inurl:authenticate.html"
        "site:${target_domain} inurl:authentication.html"
        "site:${target_domain} inurl:resetpassword.html"
        "site:${target_domain} inurl:passwordreset.html"
        "site:${target_domain} inurl:change_password.html"
        "site:${target_domain} inurl:user.html"
        "site:${target_domain} inurl:users.html"
        "site:${target_domain} inurl:member.html"
        "site:${target_domain} inurl:members.html"
        "site:${target_domain} inurl:profile.html"
        "site:${target_domain} inurl:login.jsp"
        "site:${target_domain} inurl:signin.jsp"
        "site:${target_domain} inurl:signup.jsp"
        "site:${target_domain} inurl:logon.jsp"
        "site:${target_domain} inurl:passwd.jsp"
        "site:${target_domain} inurl:password.jsp"
        "site:${target_domain} inurl:auth.jsp"
        "site:${target_domain} inurl:authenticate.jsp"
        "site:${target_domain} inurl:authentication.jsp"
        "site:${target_domain} inurl:resetpassword.jsp"
        "site:${target_domain} inurl:passwordreset.jsp"
        "site:${target_domain} inurl:change_password.jsp"
        "site:${target_domain} inurl:user.jsp"
        "site:${target_domain} inurl:users.jsp"
        "site:${target_domain} inurl:member.jsp"
        "site:${target_domain} inurl:members.jsp"
        "site:${target_domain} inurl:profile.jsp"
        "site:${target_domain} inurl:login.asp"
        "site:${target_domain} inurl:signin.asp"
        "site:${target_domain} inurl:signup.asp"
        "site:${target_domain} inurl:logon.asp"
        "site:${target_domain} inurl:passwd.asp"
        "site:${target_domain} inurl:password.asp"
        "site:${target_domain} inurl:auth.asp"
        "site:${target_domain} inurl:authenticate.asp"
        "site:${target_domain} inurl:authentication.asp"
        "site:${target_domain} inurl:resetpassword.asp"
        "site:${target_domain} inurl:passwordreset.asp"
        "site:${target_domain} inurl:change_password.asp"
        "site:${target_domain} inurl:user.asp"
        "site:${target_domain} inurl:users.asp"
        "site:${target_domain} inurl:member.asp"
        "site:${target_domain} inurl:members.asp"
        "site:${target_domain} inurl:profile.asp"
        "site:${target_domain} inurl:login.shtm"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.git"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.git/config"
        "site:${target_domain} inurl:.git/HEAD"
        "site:${target_domain} inurl:.git/index"
        "site:${target_domain} inurl:.git/refs"
        "site:${target_domain} inurl:.git/logs"
        "site:${target_domain} inurl:.git/objects"
        "site:${target_domain} inurl:.git/hooks"
        "site:${target_domain} inurl:.git/info"
        "site:${target_domain} inurl:.git/description"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.svn"
        "site:${target_domain} inurl:.svn/entries"
        "site:${target_domain} inurl:.svn/wc.db"
        "site:${target_domain} inurl:.DS_Store"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.bashrc"
        "site:${target_domain} inurl:.bash_profile"
        "site:${target_domain} inurl:.bash_logout"
        "site:${target_domain} inurl:.profile"
        "site:${target_domain} inurl:.zshrc"
        "site:${target_domain} inurl:.zprofile"
        "site:${target_domain} inurl:.zsh_history"
        "site:${target_domain} inurl:.zlogin"
        "site:${target_domain} inurl:.vimrc"
        "site:${target_domain} inurl:.viminfo"
        "site:${target_domain} inurl:.gvimrc"
        "site:${target_domain} inurl:.nvimrc"
        "site:${target_domain} inurl:.sql"
        "site:${target_domain} inurl:.sqlite"
        "site:${target_domain} inurl:.db"
        "site:${target_domain} inurl:.mdb"
        "site:${target_domain} inurl:.mysql_history"
        "site:${target_domain} inurl:.pgpass"
        "site:${target_domain} inurl:.mysql"
        "site:${target_domain} inurl:.mysql_history"
        "site:${target_domain} inurl:.pgpass"
        "site:${target_domain} inurl:.pg_history"
        "site:${target_domain} inurl:.pgadmin"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
        "site:${target_domain} inurl:.htpasswd"
        "site:${target_domain} inurl:.htaccess"
        "site:${target_domain} inurl:.env"
        "site:${target_domain} inurl:.bash_history"
        "site:${target_domain} inurl:.gitignore"
    )

    # Loop through each dorking query
    for ((i = 0; i < ${#queries[@]}; i+=3)); do
        # Open Google Chrome with the dorking queries
        for ((j = 0; j < 3 && i + j < ${#queries[@]}; j++)); do
            google-chrome "https://www.google.com/search?q=${queries[i+j]// /%20}" & disown
            echo "Opening Google Chrome with the dork query: ${queries[i+j]}"
        done

        # Prompt for confirmation before proceeding to the next set of queries
        read -p "Press [Enter] to run the next set of queries (or type 'quit' to exit): " choice
        if [[ $choice == "quit" ]]; then
            echo "Exiting..."
            return
        fi
    done

    echo "All dorking queries for ${target_domain} have been executed. Close the tabs when you're done viewing the results."
}

# Main loop to continuously prompt for dorking queries
while true; do
    perform_dorking
    read -p "Press [Enter] to perform dorking for another target domain or type 'quit' to exit: " choice
    if [[ $choice == "quit" ]]; then
        echo "Exiting..."
        break
    fi
done
